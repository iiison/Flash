import Base from '$lib/Base'

/**
 * Base View class, will add more functionality later
 */
export default class BaseView extends Base {
  /**
   * Base view constructor.
   * @return {[type]} [description]
    constructor() {
      super()
    }
   */
}
